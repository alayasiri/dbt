version: 2

models:
  - name: stg_2018_ranks
    description: one unique country per row |
                '{{ doc("factors_descriptions")}}'
    columns:
      - name: Country
        description: PK (for 2018 rankings)
        tests:
          - unique 
          - not_null
  - name: stg_2019_ranks
    description: one unique country per row
                '{{ doc("factors_descriptions")}}'
    columns:
      - name: Country
        description: PK (for 2019 rankings)
        tests:
          - unique 
          - not_null
          
          
